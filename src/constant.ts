export const AUTO_SCROLL_AFTER_USER_SCROLL = 5000;

export const LRC_LINE_CLASS_NAME = `react_lrc_line_${Math.random()
  .toString(36)
  .substring(7)}`;

export const BLANK_HEIGTH = {
  TOP: 0.4,
  BOTTOM: 0.6,
};

export const RESIZE_INTERVAL = 1000;
